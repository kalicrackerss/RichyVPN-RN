# 📱 RichyVPN React Native - Полная инструкция

## ✅ Что было сделано

Приложение Richy VPN полностью переписано на **React Native**. Это позволит вам:

1. ✅ Работать на Windows (использовать облачные сервисы для iOS)
2. ✅ Создать IPA файл для iPhone
3. ✅ Поддерживать тот же функционал как в Swift версии
4. ✅ Использовать кроссплатформенный код

## 📦 Что внутри

### Структура файлов:
```
src/
├── screens/           # Все экраны приложения
│   ├── HomeScreen.tsx         # Главный экран подключение
│   ├── ServersScreen.tsx      # Управление серверами
│   ├── SettingsScreen.tsx     # Настройки приложения
│   └── AddServerScreen.tsx    # Добавление новых серверов
│
├── contexts/          # Управление состояния (State Management)
│   └── VPNContext.tsx         # VPN логика и состояние
│
├── components/        # Переиспользуемые компоненты
│   └── GlassCard.tsx          # UI компонент Liquid Glass
│
├── utils/            # Вспомогательные функции
│   └── VLESSParser.ts        # Парсер VLESS конфигураций
│
└── types/            # TypeScript типы
    └── navigation.ts          # Типы навигации
```

## 🚀 Запуск на Windows

### Способ 1: Симулятор (если установлен Android Studio)

```bash
# Запустить Android эмулятор
npm run android
```

### Способ 2: На реальном iPhone (нужен Mac временно)

1. На Mac:
```bash
npm run ios
```

2. Подключите iPhone через USB
3. Выберите устройство в Xcode

## 🏗️ Сборка IPA файла ДЛЯ APP STORE

### Вариант 1: Codemagic (Рекомендуется) ⭐

Самый простой способ для Windows пользователей!

**Шаги:**

1. **Загрузьте на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/YOUR_USERNAME/RichyVPN_RN.git
   git push -u origin main
   ```

2. **Создайте обычну на Codemagic:**
   - Перейдите https://codemagic.io/
   - Нажмите "Sign up with GitHub"
   - Авторизуйте Codemagic доступ к вашему GitHub

3. **Подключите репозиторий:**
   - В Codemagic Dashboard нажмите "Add app"
   - Выберите `RichyVPN_RN` репозиторий
   - Нажмите "Set up build"

4. **Конфигурация уже есть:**
   - Файл `codemagic.yaml` уже в проекте
   - Codemagic автоматически прочитает его

5. **Запустите сборку:**
   - Нажмите кнопку "Start new build"
   - Выберите ветку `main`
   - Дождитесь завершения (~10-15 минут)

6. **Скачайте IPA:**
   - После сборки найдите артефакт 
   - Нажмите на `RichyVPN_RN.xcarchive`
   - IPA готов!

### Вариант 2: GitHub Actions

Если у вас есть Mac в CI/CD:

```bash
# Просто запушьте код
git push origin main

# GitHub Actions автоматически:
# 1. Установит зависимости
# 2. Соберет приложение
# 3. Создаст архив
# 4. Загрузит в Artifacts
```

Workflow находится в `.github/workflows/build-ios.yml`

### Вариант 3: Локально на Mac

Если у вас есть доступ к Mac:

```bash
# Клонируйте репоритолий
git clone https://github.com/YOUR_USERNAME/RichyVPN_RN.git
cd RichyVPN_RN

# Установите зависимости
npm install
cd ios && pod install && cd ..

# Откройте в Xcode
open ios/RichyVPN_RN.xcworkspace

# Или соберите из терминала
xcodebuild -workspace ios/RichyVPN_RN.xcworkspace \
  -scheme RichyVPN_RN \
  -configuration Release \
  -archivePath build/RichyVPN_RN.xcarchive \
  archive
```

## 🔑 Требования для App Store

Перед публикацией нужно:

1. **Apple Developer Account** ($99/год)
   - https://developer.apple.com/
   - Создайте App ID

2. **Сертификаты и Provisioning Profiles**
   - Генерируются в Developer Portal
   - Нужны для подписи приложения

3. **App Store Connect**
   - Создайте app запись
   - Загрузьте IPA через Transporter

## 📋 Функциональность приложения

### ✅ Главный экран
- Большая кнопка подключения VPN
- Анимация при подключении
- Статус подключения
- Информация о текущем сервере

### ✅ Управление серверами
- **Добавить** - через VLESS ссылку или ручной ввод
- **Список** - просмотр всех серверов
- **Удалить** - удаление с подтверждением
- **Поиск** - быстрый поиск по названию

### ✅ Добавление серверов
- **2 способа:**
  1. Вставить VLESS ссылку (vless://...)
  2. Ручной ввод (адрес, порт, UUID)
- Поддержка Reality параметров
- Валидация данных

### ✅ Настройки
- Kill Switch
- Автоподключение
- Темная тема (по умолчанию)
- Информация о версии

## 🔐 Безопасность

**Примечание:** Текущая реализация эмулирует VPN подключение.

Для **реального VPN** функционала нужно:

1. Создать Native Module (Objective-C/Swift)
2. Использовать NetworkExtension Framework
3. Создать VPN app extension

Инструкции будут в следующей версии.

## 🛠 Технические детали

### Stack
- **React Native** 0.83.1
- **React Navigation** 6.x
- **AsyncStorage** - локальное хранилище
- **TypeScript** - типизированный код

### API
- VPNContext - главный контекст состояния
- VLESSParser - парсинг конфигураций
- AsyncStorage - сохранение данных

### UI/UX
- Bottom Tab Navigation
- Stack Navigation
- Liquid Glass компоненты
- Animations (подключение)

## 🐛 Troubleshooting

### Проблема: Ошибка при npm install
**Решение:**
```bash
npm cache clean --force
npm install
```

### Проблема: Pod install ошибка
**Решение:**
```bash
cd ios
rm Podfile.lock
pod repo update
pod install
cd ..
```

### Проблема: Codemagic build фэйлится
**Решение:**
1. Проверьте codemagic.yaml синтаксис
2. Убедитесь что все зависимости в package.json
3. Проверьте iOS Deployment Target

## 📚 Документация

- [BUILD_IPA.md](./BUILD_IPA.md) - Детальные инструкции по сборке IPA
- [CODEMAGIC_SETUP.md](./CODEMAGIC_SETUP.md) - Настройка Codemagic
- [README.md](./README.md) - Основная информация

## 💡 Полезные команды

```bash
# Развитие
npm start                 # Запустить Metro
npm run ios              # Запустить iOS симулятор
npm run android          # Запустить Android эмулятор

# Сборка
npm run build:ios        # Собрать для iOS
npm run build:android    # Собрать для Android

# Лinting и типы
npm run lint             # Проверка кода
npm run type-check       # Проверка TypeScript

# Очистка
npm run clean            # Очистить cache
```

## 🎯 Что дальше?

### На Windows:
1. ✅ Загрузите код на GitHub
2. ✅ Используйте Codemagic для сборки
3. ✅ Скачайте IPA файл
4. ✅ Опубликуйте на App Store

### На Mac (опционально):
1. Локальная разработка
2. Локальная сборка
3. Тестирование на реальном устройстве

## 🤝 Контакты

Если у вас есть вопросы:
- Откройте Issue на GitHub
- Посмотрите документацию
- Проверьте Codemagic Logs

---

## ✨ Итоговую:

✅ **React Native приложение готово к использованию!**

От Windows пользователя нужно:
1. Загрузить на GitHub
2. Зарегистрироваться на Codemagic
3. Нажать кнопку Build
4. Скачать готовый IPA

**Все остальное Codemagic сделает автоматически на облачном Mac!**

🎉 **Готово к App Store!**
